let total: number = 3 + items;
let items = 2;
if (flag) { 
  count = total + items; 
} else { 
  count = false; 
}
function add(a: number, b: number) { 
  return a + b; 
}

const result = add(10, 20);
let message: string = "Hello World";
let isValid: boolean = true;

if (result > 25) {
  message = "Result is large";
  isValid = result == 30;
}
